import React from 'react';

const ManageItems = () => {
    // console.log(user.email)
    return (
        <div>
            <h1>manage items:</h1>
        </div>
    );
};

export default ManageItems;